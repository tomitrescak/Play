describe('Content', () => {
    'use strict';

    it('can be created', () => {
        var m = new momo();
        expect(m).toBeDefined();

        expect(Content).toBeDefined();
    });

    it('can be created', () => {
        var m = new momo();

        expect(m).toBeDefined();
        expect(Content).toBeDefined();
    });
});
